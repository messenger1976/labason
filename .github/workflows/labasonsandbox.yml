on:
  push:
    branches:
      - main
name: 🚀 Deploy labason Sandbox water district system on push
jobs:
  web-deploy:
    name: 🎉 Deploy labason sandbox water billing Server
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: sandbox.labwd.com
        username: ${{ secrets.ftp_username1 }}
        password: ${{ secrets.ftp_password1 }}
        server-dir: /
        exclude: |
          **/application/cache/**
          **/application/config/database.php
          **/.buildpath/**
          .ftp-deploy-sync-state.json
          .git-ftp.log
          .project
          .htaccess
          error_log