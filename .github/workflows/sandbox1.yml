on:
  push:
    branches:
      - sandbox1
name: 🚀 Deploy labason Sandbox1 water district system on push
jobs:
  web-deploy:
    name: 🎉 Deploy labason sandbox1 water billing Server
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v2
    
    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@4.3.0
      with:
        server: sandbox1.labwd.com
        username: ${{ secrets.ftp_username2 }}
        password: ${{ secrets.ftp_password2 }}
        server-dir: /
        exclude: |
          **/application/cache/**
          **/application/config/database.php
          **/.buildpath/**
          .ftp-deploy-sync-state.json
          .git-ftp.log
          .project
          .htaccess
          error_log